# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
venv312/

# Local Python pin (3.14) — kept out of the repo so Streamlit Cloud, which has
# no 3.14 yet, can build on 3.13 (see requires-python in pyproject.toml).
.python-version

# Secrets
.env
.streamlit/secrets.toml

# Claude Code local (personal) settings
.claude/settings.local.json
# Skill symlinks auto-generated into the workspace (point into .venv)
.claude/skills/
.agents/

# Source data and generated vector index (regenerate with `python -m src.ingest`)
data/raw/
vectorstore/

